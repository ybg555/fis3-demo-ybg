项目目录直接按照服务器端的路径架构

1，静态资源MD5 D
2，SCSS编译 D
3, 静态资源压缩混淆 D
4, 模板编译 D
https://www.npmjs.com/package/fis-parser-velocity
5，模板数据 D
6，页面模块化 D
（7注：用模板编译插件会自动载入文件夹内同名静态文件,fis3的inline需要手动载入模块的静态资源）
7，静态资源打包合并(打包后需要release产出一次)  D
8，静态资源自定义路径输出 (分组匹配加上$1) D
9，js模块化，只负责声明官方化而已，require.js还是需要自己引入 D
10,$0表示原样输出，$1原文件名输出，常用配合是分组() D